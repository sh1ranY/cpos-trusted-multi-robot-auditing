# Scripts read exported environment variables; this file is not loaded automatically.
RPC_URL=http://127.0.0.1:8545
DEPLOYMENT_FILE=deployments/local.json
# PRIVATE_KEY defaults to the publicly known Hardhat local test account #0.
# Use only a disposable local development chain.
